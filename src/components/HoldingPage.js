import React from 'react';

const HoldingPage = ({ title }) => {
  return (
    <div style={{ textAlign: 'center', margin: '50px' }}>
      <h1>{title}</h1>
      <p>This page is currently under construction.</p>
    </div>
  );
};

export default HoldingPage;
